---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
  labels:
    name: app-ingress
spec:
  ingressClassName: traefik
  rules:
    - host: osiruko.ayane0857.net
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: ayane-osiruko
                port:
                  number: 80
